

server:
  servlet:
    context-path: /api
spring:
  application:
     name: app
  profiles:
     active: dev
  rabbitmq:
    username: guest
    password: guest
    port: 5672
    host: localhost
  jpa:
    open-in-view: false
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path:  /swagger-ui.html